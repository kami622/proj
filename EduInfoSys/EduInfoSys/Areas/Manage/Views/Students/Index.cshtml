@model List<GroupStudent>
@{
	int count = 0;
}
<div class="row mb-3">
	<div class="col-6">
		<h1 class="h3">Students</h1>
	</div>
	<div class="col-6 d-flex justify-content-end">
		<a asp-action="create" class="btn btn-primary text-center"><i class="align-middle me-1" data-feather="plus-circle"></i>Add new</a>
	</div>
</div>

<div class="card">
	<div class="card-body">
		<table id="datatables-reponsive" class="table table-striped" style="width:100%">
			<thead>
				<tr>
					<th scope="col">#</th>
					<th scope="col">Full name</th>
					<th scope="col">Group</th>
					<th scope="col">Actions</th>
				</tr>
			</thead>
			<tbody>
				@foreach (GroupStudent item in Model)
				{
					count++;
					<tr>
						<th scope="row">@count</th>
						<td>@item.User.FirstName @item.User.LastName [@item.User.UserName]</td>
						<td>@item.Group.Name [@item.Group.Department.Name]</td>
						<td><a asp-action="update" asp-route-id="@item.Id" class="btn btn-primary"><i class="align-middle" data-feather="edit-2"></i></a> <a asp-action="delete" asp-route-id="@item.Id" class="btn btn-danger btn-delete"><i class="align-middle" data-feather="trash-2"></i></a></td>
					</tr>
				}
			</tbody>
		</table>
	</div>
</div>